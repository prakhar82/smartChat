<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2025 SmartChat Contributors
  ~ All rights reserved.
  ~ Unauthorized copying or distribution of this file,
  ~ via any medium, is strictly prohibited unless permitted by license.
  ~ Author: $USER_NAME
  -->

<!--
  ===============================================================
  🧱 SmartChat :: Common Library (Production-Ready)
  ===============================================================
  © 2025 SmartChat Contributors. All rights reserved.
  Unauthorized copying or distribution is prohibited.
  ===============================================================
  Maintainers: SmartChat Backend Team <team@smartchat.app>
  Description : Shared DTOs, configuration, caching, and utilities
                used by all SmartChat microservices.
  ===============================================================
-->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <!-- ==================================================== -->
    <!-- 📦 Parent Definition (inherits dependency management) -->
    <!-- ==================================================== -->
    <parent>
        <groupId>com.smartchat</groupId>
        <artifactId>backend</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>smartchat-common</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>SmartChat :: Common Library</name>
    <description>
        Provides shared configuration, cache utilities, DTOs, and
        constants for all SmartChat backend modules.
    </description>

    <!-- ==================================================== -->
    <!-- 🧩 Dependencies -->
    <!-- ==================================================== -->
    <dependencies>

        <!-- ✅ Core Spring Boot Utilities -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>

        <!-- ✅ JSON Processing (Jackson Databind) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- ✅ Jackson support for Java 8+ date/time (Instant, LocalDate, etc.) -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- ✅ Caching and Redis Support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Spring Boot Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- ✅ JWT Authentication -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <scope>runtime</scope>
        </dependency>


        <!-- ✅ JPA & Repository Support (for CrudRepository in cache layer) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- ✅ Validation API (used in DTOs) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ✅ Lombok (compile-time annotation processing) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- ✅ Optional Hibernate Types (for JSON and Array mapping in PostgreSQL) -->
        <dependency>
            <groupId>com.vladmihalcea</groupId>
            <artifactId>hibernate-types-60</artifactId>
            <version>2.21.1</version>
            <optional>true</optional>
        </dependency>

        <!-- ✅ Spring Boot Configuration Processor (for @ConfigurationProperties) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ✅ For WebMvcConfigurer, CorsRegistry, and MVC-level CORS handling -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- existing deps (security, lombok, etc.) -->

    </dependencies>

    <!-- ==================================================== -->
    <!-- ⚙️ Build Configuration -->
    <!-- ==================================================== -->
    <build>
        <finalName>smartchat-common</finalName>

        <plugins>

            <!-- 🧱 Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                    <compilerArgs>
                        <arg>-Xlint:deprecation</arg>
                        <arg>-Xlint:unchecked</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- 🚫 Disable Spring Boot repackage (this is a library, not a runnable JAR) -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <!-- ==================================================== -->
    <!-- 🌍 Profiles -->
    <!-- ==================================================== -->
    <profiles>

        <!-- 👩‍💻 Development Profile -->
        <profile>
            <id>dev</id>
            <properties>
                <spring.profiles.active>dev</spring.profiles.active>
            </properties>
        </profile>

        <!-- 🚀 Production Profile -->
        <profile>
            <id>prod</id>
            <properties>
                <spring.profiles.active>prod</spring.profiles.active>
            </properties>
        </profile>

    </profiles>

</project>
